# yzbot Phase 1 realtime demo — slim runtime deps (no torch/chromadb/sentence-transformers).
# RAG/memory degrade gracefully at startup (Phase 2 reconnects them with the heavy deps).
fastapi==0.109.2
uvicorn[standard]==0.27.1
websockets==12.0
pydantic==2.6.1
python-multipart==0.0.9
python-dotenv==1.0.1
PyYAML==6.0.1
numpy==1.26.4
openai==1.12.0
dashscope>=1.20.0
# Deprecated voice path imports these lazily inside functions; kept for safety.
edge-tts>=6.1.9
SpeechRecognition==3.10.1
